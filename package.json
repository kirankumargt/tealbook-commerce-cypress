{
  "name": "tealbook-commerce-tests",
  "version": "1.0.0",
  "description": "E2E tests",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:open:desktop": "cypress open --env viewportType=desktop --config-file cypress.json --config viewportWidth=1000,viewportHeight=660",
    "cypress:run:desktop:e2e:chrome": "cypress run --env viewportType=desktop --browser chrome --config-file cypress.json --config viewportWidth=1000,viewportHeight=660 --spec 'cypress/integration/e2e-tests/*/*.spec.ts'"
  },
  "author": "Kiran Kumar GT",
  "license": "ISC",
  "dependencies": {
    "cypress-wait-until": "^1.7.2",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@cypress/request": "^2.88.10",
    "@faker-js/faker": "^6.2.0",
    "@types/node": "^17.0.29",
    "axios": "^0.27.2",
    "cypress": "^9.6.0",
    "cypress-mochawesome-reporter": "^3.0.1",
    "eslint-plugin-cypress": "^2.12.1",
    "mocha": "^9.2.2",
    "mochawesome": "^7.1.3",
    "mochawesome-report-generator": "^6.2.0"
  }
}
